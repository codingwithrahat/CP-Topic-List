#include<bits/stdc++.h>
using namespace std;

int main(){

   vector<int>v{1, 2, 3}; 

   vector<int>v2(v) //copy vector v into v2;
   //copy constructor only work for same type (like vector to vector)

   vector<int>v3 = v; //both are same

}
