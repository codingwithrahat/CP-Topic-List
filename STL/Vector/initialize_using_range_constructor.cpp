#include<bits/stdc++.h>
using namespace std;

int main(){

   set<int>s;

   s.insert(1);
   s.insert(2);
   s.insert(3);

   vector<int>v(s.begin(), s.end());

   //Copies all elements from the set into a vector.
   //TC - O(n), n is size of set

}
